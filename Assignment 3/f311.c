#include<stdio.h>
int main()
{
    int a,b,c,d,e,x;
    printf("Enter a num");
    scanf("%d",&x);
  
    if (a>=33 && b>=33 && c>=33 && d>=33 && e>=33)
    printf("pass",x);
    else
    printf("faild",x);
    return 0;
}